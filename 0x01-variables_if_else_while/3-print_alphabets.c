#include <stdio.h>

/**
 * main -  print ch in lowercase and uppercase
 * description - ch followed by new line \n
 * return: always 0 (succes)
 */

int main(void)
{
	char ch;

	for( ch = a; ch <= 'z'; ch++)
	{
		puchat(ch);
	for( ch = A; ch < 'Z'; ch++)
	{
		putchar(ch);
	}
	puchar(\n);
	}
		return(0);
}

